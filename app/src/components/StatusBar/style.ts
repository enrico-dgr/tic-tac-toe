import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  connected: {
    color: 'green'
  },
  notConnected: {
    color: 'red'
  },
  common: {
    fontSize: 30,
    position: 'absolute',
    right: 30,
    top: 30
  }
});
